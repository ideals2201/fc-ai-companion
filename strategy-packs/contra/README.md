# Contra Strategy Pack

本目录用于保存 `gameProfileId = contra` 的正式 StrategyPack 文件。

当前状态：

- 目录已建立。
- Contra US 第一关示例仍在 `references/contra-us/strategy-db/contra-us-stage1-strategy-pack-example.md`。
- 下一步需要按标准拆成实际 JSON 文件后迁入本目录。

本目录不得包含 ROM 文件。
